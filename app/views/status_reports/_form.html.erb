<%= simple_form_for([@project, @status_report], html: {class: 'form-horizontal' }) do |f| %>
  <%= f.input :reported_on %>
  <%= f.input :summary %>
  <%= f.input :overall %>
  <%= f.button :submit, "Save" %>
<% end %>